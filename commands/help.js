const Discord = require("discord.js");

exports.run = (client, message, args) => {
    let help = new Discord.MessageEmbed()
        .setTitle("Help Menu")
        .setDescription("These are the commands we currently have. run >suggest to suggest one!")
}